<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="GameMapBackgroundBrush" Color="#101312" />
    <SolidColorBrush x:Key="GameMapBorderBrush" Color="#25332D" />
    <SolidColorBrush x:Key="GameHudBrush" Color="#F3FFF6" />
    <SolidColorBrush x:Key="GameButtonIdleBrush" Color="#202622" />
    <SolidColorBrush x:Key="GameButtonIdleBorderBrush" Color="#5B6A60" />
    <SolidColorBrush x:Key="GameButtonPressedBrush" Color="#24E454" />
    <SolidColorBrush x:Key="GameButtonPressedBorderBrush" Color="#B8FFC2" />

    <Style x:Key="DirectionButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="58" />
        <Setter Property="Height" Value="58" />
        <Setter Property="Margin" Value="1" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <ContentPresenter HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
